# Local development uses Wrangler and local D1 state. Docker Compose is no longer
# a supported runtime because D1 is a Cloudflare-managed database binding.
#
# Start the control-plane Worker with:
#   npm run dev
#
# Apply the local schema with:
#   npx wrangler d1 migrations apply wazoo-api --local
